# Handoff: Chunk 6 complete — race state store and `/api/state`

## Metadata

- Created: 2026-05-16 20:55 Asia/Dubai.
- Project path: `c:\Users\abiali\Documents\Academic\Projects\Live Race Dash`.
- Continues from: `.cline/handoffs/2026-05-16-203000-chunk-5-3-complete.md`.
- Purpose: record completion of Milestone 1 Chunk 6.

## Current State Summary

- **Chunk 6 is complete.**
- Added the backend's central in-memory race state store.
- Added `GET /api/state`, currently returning the default/empty race state.
- Existing `/health` and `/api/metadata` endpoints still work.
- No WIGE LiveTiming WebSocket connection or message normalization has been added yet.

## Important Context

- The project remains backend-first and learning-focused.
- Current backend endpoints:
  - `GET /health`
  - `GET /api/metadata`
  - `GET /api/state`
- Eventhub metadata is loaded from local snapshot `backend/data/bootstrap` and currently returns `count: 161`, `withImages: 161`.
- Race state is intentionally empty until WIGE ingestion/normalization chunks are implemented.
- Milestone 1 remains in-memory only; no database has been added.

## Changes Made

### Added `backend/src/raceState.ts`

- Imports existing `RaceState` and `RaceStateCounters` types.
- Defines `createInitialCounters()` returning:
  ```json
  {
    "pid0": 0,
    "pid3": 0,
    "pid4": 0,
    "pid9002": 0,
    "other": 0
  }
  ```
- Defines `createInitialRaceState()` returning:
  ```json
  {
    "connected": false,
    "lastUpdate": null,
    "session": null,
    "trackState": null,
    "cars": [],
    "messages": [],
    "stats": null,
    "counters": { ... }
  }
  ```
- Stores state in module memory:
  ```ts
  let raceState = createInitialRaceState();
  ```
- Exports:
  - `getRaceState()` — returns `structuredClone(raceState)`.
  - `resetRaceState()` — resets to initial state and returns a cloned state.

### Updated `backend/src/server.ts`

- Imported `getRaceState` from `./raceState.js`.
- Added:
  ```ts
  app.get("/api/state", (_req, res) => {
    res.json(getRaceState());
  });
  ```

## Decisions Made

- Chunk 6 only creates the state container and endpoint; WIGE socket logic remains deferred.
- `getRaceState()` returns a cloned object to avoid callers accidentally mutating internal state.
- No Eventhub metadata is joined into `RaceState.cars` yet because no timing rows exist yet.
- Future chunks should add specific update functions rather than mutating `raceState` directly from outside the module.

## Verification Performed

1. TypeScript check:
   ```powershell
   cd backend; npm run typecheck
   ```
   Result: passed with no TypeScript errors.

2. Temporary backend endpoint test:
   - Started backend with `npm start` in a temporary PowerShell job.
   - Verified `/health` returns OK with `eventId: 50`.
   - Verified `/api/metadata` still returns `count: 161`, `withImages: 161`, `error: null`, and car `80` exists.
   - Verified `/api/state` returns:
     ```json
     {
       "connected": false,
       "lastUpdate": null,
       "session": null,
       "trackState": null,
       "cars": [],
       "messages": [],
       "stats": null,
       "counters": {
         "pid0": 0,
         "pid3": 0,
         "pid4": 0,
         "pid9002": 0,
         "other": 0
       }
     }
     ```

3. Port cleanup check:
   ```powershell
   Get-NetTCPConnection -LocalPort 3001 -State Listen
   ```
   Result: no process listening on port 3001.

## Immediate Next Steps

1. Start **Chunk 7 — WIGE normalizer functions**.
2. Before implementing, inspect WIGE capture artifacts:
   - `exploration-archive/captures/livetiming-ws-event-50-2026-05-14T15-40-55-034Z.jsonl`
   - `exploration-archive/captures/livetiming-ws-summary.txt`
3. Create `backend/src/normalizers.ts`.
4. Implement safe parsing helpers and normalizers for observed WIGE PIDs, especially PID 0 timing rows.
5. Keep this offline/testable against captured JSON before connecting to the live WIGE websocket.

## Critical Files

- `backend/src/raceState.ts`
- `backend/src/server.ts`
- `backend/src/types.ts`
- `backend/src/eventhubMetadata.ts`
- `.cline/handoffs/2026-05-16-205500-chunk-6-complete.md`

## Gotchas / Risks

- Do not add WIGE live socket logic until normalizers/state update functions are ready.
- `structuredClone` is supported in the current Node runtime; if the runtime changes to an older Node version, revisit cloning.
- Keep state in memory only for Milestone 1.
- Frontend should still not connect directly to WIGE/Eventhub.
- Do not overpromise real car GPS.

## Pending Work / Backlog

- Chunk 7: normalizers for WIGE messages.
- Chunk 8: WIGE LiveTiming websocket client and reconnect behavior.
- Chunk 9: debug/status endpoint.
- Later: frontend dashboard after backend state endpoint has useful live data.