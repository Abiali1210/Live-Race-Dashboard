# Handoff: Chunk 13A–13B progress

## Metadata

- Created: 2026-05-17 16:37 Asia/Dubai.
- Project path: `c:\Users\abiali\Documents\Academic\Projects\Live Race Dash`.
- Continues from: `.cline/handoffs/2026-05-17-153600-chunk-13-dashboard-shell-plan.md`.
- Related side-task handoff: `.cline/handoffs/2026-05-17-162700-wige-playback-recorder.md`.
- Purpose: record completed progress for Chunk 13A and 13B before continuing with 13C.

## Current State Summary

- Chunk 13A is complete: a dedicated Nürburgring `TrackMap` component exists.
- Chunk 13B is complete: frontend polling/data state is cleaner and preserves last good data on refresh failure.
- The WIGE playback recorder side task is complete and currently may be running in the user’s background terminal. Do not interrupt it.
- Next implementation step is Chunk 13C: map-led dashboard layout.

## Changes Completed

### Chunk 13A — Track map component foundation

Added:

```text
frontend/src/components/TrackMap.tsx
frontend/src/components/TrackMap.css
```

Key behavior:

- Imports local SVG source:
  ```ts
  import sourceSvg from "../../Circuit_Nürburgring-2002-24h.svg?raw";
  ```
- Parses the SVG with `DOMParser`.
- Extracts:
  - main 24h circuit outline
  - secondary route/detail paths
  - pit/connector paths
  - direction markers
  - start/finish checker tiles
- Renders inline SVG for direct styling.
- Adds curated labels only:
  - Nordschleife
  - GP-Strecke
  - Start/Ziel
  - Döttinger Höhe
- Adds hover/focus glow styling and reduced-motion support.
- Includes attribution and explicit no-GPS note.

Verification:

```powershell
cd frontend
npm run build
npm run lint
```

Both passed after 13A.

### Chunk 13B — App data/state cleanup

Updated:

```text
frontend/src/App.tsx
```

Key behavior:

- Keeps existing local backend polling every `5000ms`.
- Still fetches only:
  - `GET /api/status`
  - `GET /api/state`
- Preserves last successful dashboard data if a later refresh fails.
- Initial load failure still shows blocking backend error.
- Later refresh failure shows non-blocking warning while stale data remains visible.
- Tracks `lastSuccessfulRefreshAt`.
- Adds derived `dashboardView` for future layout work:
  - `leaderboardCars` top 15
  - `featuredCar`
  - `recentMessages`
  - `backendConnected`
  - `wigeConnected`
  - `refreshedAtLabel`

Verification:

```powershell
cd frontend
npm run build
npm run lint
```

Both passed after 13B.

## Side Task Completed

Added WIGE playback recorder:

```text
backend/src/recordWigePlayback.ts
```

Updated:

```text
backend/package.json
.gitignore
```

Purpose: capture raw WIGE websocket packets into `backend/playback/` for future offline replay after the race ends.

Important: user started the recorder in the background. Avoid commands that would interrupt it, delete `backend/playback/`, or kill Node/npm processes.

## Decisions Made

- Keep `TrackMap` as a separate component before wiring it into the dashboard.
- Use raw SVG import and runtime parsing to avoid hardcoding a huge path constant.
- Keep all frontend data flow local-backend-only.
- Make stale-data behavior explicit rather than blanking the dashboard on temporary refresh failure.
- Do not build playback mode yet; recorder only captures data.

## Critical Files

- `frontend/src/components/TrackMap.tsx`
- `frontend/src/components/TrackMap.css`
- `frontend/src/App.tsx`
- `frontend/Circuit_Nürburgring-2002-24h.svg`
- `backend/src/recordWigePlayback.ts`
- `backend/package.json`
- `.gitignore`
- `.cline/handoffs/2026-05-17-162700-wige-playback-recorder.md`

## Gotchas

- Do not interrupt the running WIGE recorder.
- Do not delete `backend/playback/` while the recorder is running.
- `TrackMap` depends on the local SVG structure. If the SVG source changes, verify `extractTrackGeometry()`.
- `TrackMap` is not yet mounted in `App.tsx`; this is expected before 13C.
- `featuredCar` and `recentMessages` are derived but not yet displayed; this is expected before 13D.

## Immediate Next Steps

1. Start Chunk 13C: map-led dashboard layout.
2. Read current:
   ```text
   frontend/src/App.tsx
   frontend/src/App.css
   frontend/src/components/TrackMap.tsx
   frontend/src/components/TrackMap.css
   ```
3. Import and place `TrackMap` in the dashboard as the largest visual panel.
4. Restructure the temporary preview into a shell with:
   - top command/status bar
   - leaderboard/messages column
   - large track map panel
   - lower/side panels reserved for featured car, track state, diagnostics
5. Keep data behavior from 13B unchanged.
6. Run frontend-only verification:
   ```powershell
   cd frontend
   npm run build
   npm run lint
   ```

## Pending Work

- 13C: map-led dashboard layout.
- 13D: first read-only panels.
- 13E: styling and verification pass.
- Future: playback reader/backend mode using recorded `packets.ndjson`.