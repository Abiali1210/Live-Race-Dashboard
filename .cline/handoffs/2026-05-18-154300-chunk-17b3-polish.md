# Handoff: Chunk 17B.3 dashboard polish

## Metadata

- Created: 2026-05-18 15:43 Asia/Dubai.
- Project path: `c:\Users\abiali\Documents\Academic\Projects\Live Race Dash`.
- Continues from: `.cline/handoffs/2026-05-18-011000-chunk-17b-progress.md`.
- Important note: this handoff also incorporates newer pasted-chat context that superseded the previous written handoff.

## Current State Summary

- Chunk 17B.3 evolved from minor CSS tuning into an iterative frontend layout correction for a race-control-style dashboard.
- The accepted structure is now:
  - compact header/status toolbar;
  - primary first-viewport dashboard with leaderboard on the left and circuit map + selected car on the right;
  - secondary section below for Race Control/messages, Track State, and Backend Diagnostics.
- The latest pass was intentionally tiny and CSS-only in `frontend/src/App.css`:
  - leaderboard scrollbars were made thinner and subtler while keeping accent/red styling;
  - selected-car image/placeholder size was increased slightly;
  - circuit map stage padding was reduced and SVG scale increased slightly while staying clipped in the card.
- A final follow-up CSS-only scrollbar tweak made the leaderboard scrollbars sleeker:
  - WebKit scrollbar width/height reduced to `2px`;
  - track remains transparent;
  - thumb is low-opacity by default and becomes more visible on hover/focus-within.
- A final CSS-only selected-car image tweak removed the forced image-frame height that caused top/bottom blank bars:
  - the actual car `<img>` now renders at intrinsic aspect ratio with `height: auto` and a `max-height` cap;
  - the image column was widened slightly so the car remains larger than before;
  - the no-image placeholder keeps a deliberate `16 / 9` frame.
- A final CSS-only circuit map caption/placement tweak tightened the attribution area and nudged the SVG down slightly:
  - dashboard map stage padding is now `4px 2px 1px`;
  - map SVG uses `translateY(1.5px) scale(1.14)`;
  - dashboard map caption margin/gap/line-height/opacity were reduced to consume less vertical space.
- A small selected-car image hover interaction was added as the first test of the dashboard neon treatment:
  - applies only to `.featured-car img` on hover-capable fine pointers;
  - uses a restrained accent/red border glow and `scale(1.01)`;
  - avoids upward translation so the image frame top border does not clip on hover;
  - adds `2px` top breathing room on `.featured-car__hero` so the top glow has room to render;
  - respects reduced-motion by disabling the transform.
- A CSS-only primary panel hover treatment was added:
  - applies only to `.dashboard-primary` leaderboard, map, and selected-car panels;
  - uses a subtle cyan-tinted border/background/outer glow;
  - does not scale, move, resize, or restructure panels;
  - selected-car image hover remains the stronger red-focused interaction.
- Frontend verification passed after the latest polish pass.

## Important Context

- Product goal remains a race-viewer-focused dashboard for ADAC RAVENOL 24h Nürburgring 2026.
- Frontend must consume only local backend endpoints (`/api/status`, `/api/state`, and `/api/metadata` only if needed).
- Do not add fake GPS, fake car positions, or implied live track locations.
- Do not restructure the dashboard again unless the user explicitly requests it; the current work should stay in targeted polish/tuning mode.
- The user specifically asked not to reread the entire large `frontend/src/App.css` during tiny CSS polish passes; prefer targeted selector searches/snippet reads.

## Decisions Made

- Kept secondary panels below the first viewport instead of squeezing every widget into `100vh`, because readability matters more than forcing all panels on screen.
- Kept selected-car internal scrolling acceptable as a safety valve, but tuned the card to be compact and useful at a glance.
- Preserved a tiny map attribution line while removing the larger explanatory “static circuit reference / no GPS” line during the previous chat, because the map card should prioritize the track SVG.
- Used small CSS adjustments rather than a structural rewrite for the final polish, to avoid destabilizing the now-acceptable layout.

## Latest Verification

Ran successfully after the final tiny CSS polish, final scrollbar tweak, final selected-car image aspect-ratio tweak, final map caption/placement tweak, selected-car image hover interaction, and primary panel hover treatment:

```powershell
cd frontend
npm run build
npm run lint
```

Observed output included successful Vite production build and ESLint completion with no reported errors.

## Current Working Tree Notes

`git status --short` after verification showed uncommitted frontend Chunk 17 work, including:

```text
M frontend/src/App.css
M frontend/src/App.tsx
M frontend/src/components/TrackMap.tsx
M frontend/src/components/dashboard/FeaturedCarPanel.tsx
M frontend/src/components/dashboard/LeaderboardControls.tsx
M frontend/src/components/dashboard/LoadingDashboard.tsx
M frontend/src/components/dashboard/MetricCard.tsx
?? .cline/handoffs/2026-05-17-235600-chunk-17-viewer-first-dashboard-plan.md
?? .cline/handoffs/2026-05-18-011000-chunk-17b-progress.md
?? frontend/src/components/dashboard/FeedStatusSummary.tsx
```

Also note: Git warned that `frontend/src/components/TrackMap.tsx` LF may be replaced by CRLF next time Git touches it.

## Critical Files

- `frontend/src/App.css` — primary layout/styling surface and latest CSS-only polish target.
- `frontend/src/App.tsx` — dashboard split into `dashboard-primary` and `dashboard-secondary`.
- `frontend/src/components/dashboard/FeaturedCarPanel.tsx` — compact selected-car card structure.
- `frontend/src/components/dashboard/LeaderboardControls.tsx` — compact count chip replacing the previous bulky count box.
- `frontend/src/components/TrackMap.tsx` — simplified attribution text.
- `frontend/src/components/dashboard/FeedStatusSummary.tsx` — compact feed summary in header/status area.

## Immediate Next Steps

1. Perform visual browser QA only if the user wants further tuning:
   - leaderboard scrollbars should be visibly thinner and accent-colored;
   - selected-car image should be slightly larger but still contained;
   - circuit map should look larger inside its card and not overlap anything;
   - secondary panels should remain below and readable.
2. If visual issues remain, make only targeted CSS edits in `frontend/src/App.css`; avoid structural rewrites.
3. Re-run frontend verification after any further edits:
   ```powershell
   cd frontend
   npm run build
   npm run lint
   ```
4. When the user is satisfied, consider committing the stable Chunk 17 frontend work.

## Potential Gotchas / Risks

- The written handoff chain was stale until this file; the pasted previous-chat context was the true source of latest design decisions.
- Avoid broad reads/rewrites of `App.css` unless genuinely necessary; use selector-targeted inspection.
- Backend may open a real WIGE websocket when started; avoid commands that kill Node/npm processes or interrupt a possible WIGE recorder.
- `MetricCard.tsx` remains modified even though the current dashboard likely no longer centers on large metric cards; do not delete or revert without checking intent.
- The local track SVG has attribution/licensing obligations; keep attribution present, even if tiny.

## Pending Work / Backlog

- Browser visual QA for Chunk 17B.3 polish.
- Possible 17B.4 responsive cleanup if the desktop polish causes small-screen issues.
- Later Chunk 17C/17D work may focus on selected-car inspector polish and final responsive UX verification.