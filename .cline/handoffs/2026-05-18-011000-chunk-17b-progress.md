# Handoff: Chunk 17B progress / before 17B.3

## Metadata

- Created: 2026-05-18 01:10 Asia/Dubai.
- Project path: `c:\Users\abiali\Documents\Academic\Projects\Live Race Dash`.
- Continues from: `.cline/handoffs/2026-05-17-235600-chunk-17-viewer-first-dashboard-plan.md`.
- Purpose: preserve the real current state after an interrupted 17B session and before starting 17B.3.

## Current State Summary

- Chunk 17A is implemented in the working tree: the large metric strip was demoted into a compact header-level `FeedStatusSummary` near the Backend/WIGE connection pills.
- Chunk 17B.1 is implemented: `App.tsx` renders panels in viewer-flow order: leaderboard, map, selected/featured car, messages, track state, diagnostics.
- Chunk 17B.2 is implemented in `App.css`: desktop/tablet/mobile grid areas place the selected-car panel closer to the map and before messages on narrow screens.
- Verification from the previous session: frontend `npm run build` and `npm run lint` passed after 17A and after 17B.1. 17B.2 was reported as saved after an interruption; run build/lint again before or after 17B.3.
- This handoff was created because an earlier attempt to update the handoff with `apply_patch` failed due to incomplete patch sentinels/workspace instability.

## Important Context

- Product goal remains a race-viewer-focused dashboard for ADAC RAVENOL 24h Nürburgring 2026.
- Frontend must keep local-backend-only data flow: `GET /api/status` and `GET /api/state`; no direct WIGE/Eventhub frontend calls.
- No fake GPS, fake track positions, or implied live car locations on the static track map.
- Keep the restrained dark race-control visual identity.
- Use `impeccable` as the primary UI workflow guidance if active/available; if a skill cannot be reactivated in the same task, read supporting skill/reference files directly instead of trying to bypass tool restrictions.

## Decisions Made

- Broke 17B into smaller substeps after the user requested simplification.
- 17B.1 was intentionally DOM/order only, to improve logical and assistive-tech flow before CSS layout changes.
- 17B.2 was intentionally CSS-only, to visually move the selected-car panel into the map/right-context area without touching data logic.
- 17B.3 should remain a small CSS tuning pass, not a broader redesign.

## Current Verified File State

Observed before creating this handoff:

- `frontend/src/App.tsx` dashboard order:
  1. `LeaderboardPanel`
  2. map panel / `TrackMap`
  3. `FeaturedCarPanel`
  4. `MessagesPanel`
  5. `TrackStatePanel`
  6. `DiagnosticsPanel`
- Desktop grid in `frontend/src/App.css`:
  - row 1: `leaderboard leaderboard map`
  - row 2: `leaderboard leaderboard featured`
  - row 3: `messages track diagnostics`
- `@media (max-width: 1280px)` grid:
  - `leaderboard leaderboard`
  - `map featured`
  - `messages messages`
  - `track diagnostics`
- `@media (max-width: 900px)` grid:
  - `map`
  - `leaderboard`
  - `featured`
  - `messages`
  - `track`
  - `diagnostics`

## Working Tree Snapshot

`git status --short` showed:

```text
M frontend/src/App.css
M frontend/src/App.tsx
 M frontend/src/components/dashboard/LoadingDashboard.tsx
 M frontend/src/components/dashboard/MetricCard.tsx
?? .cline/handoffs/2026-05-17-235600-chunk-17-viewer-first-dashboard-plan.md
?? frontend/src/components/dashboard/FeedStatusSummary.tsx
```

Note: `MetricCard.tsx` was described as deleted/unused in the previous 17A summary, but current status displayed it as modified. Verify exact diff/status before committing. Do not assume it is safely removed until checked.

## Immediate Next Steps

1. Start **17B.3 — height and density tuning**, CSS-only unless a clear structural issue is found.
2. Inspect/tune these CSS targets:
   - `.map-panel`
   - `.dashboard-track-map .track-map__stage`
   - `.leaderboard-table-wrap`
   - `.featured-panel`
   - `.featured-car img`
   - `.featured-car__image-placeholder`
3. Keep the goal narrow:
   - right-column balance between map and selected-car inspector;
   - leaderboard scroll height/cap so the first viewport does not become awkward;
   - no new data logic and no new feature scope.
4. Run frontend verification:
   ```powershell
   cd frontend
   npm run build
   npm run lint
   ```
5. After 17B.3, proceed to 17B.4 responsive cleanup only if needed.

## Critical Files

- `frontend/src/App.tsx`
- `frontend/src/App.css`
- `frontend/src/components/dashboard/FeedStatusSummary.tsx`
- `frontend/src/components/dashboard/FeaturedCarPanel.tsx`
- `frontend/src/components/dashboard/LoadingDashboard.tsx`
- `frontend/src/components/dashboard/MetricCard.tsx`
- `.cline/handoffs/2026-05-17-235600-chunk-17-viewer-first-dashboard-plan.md`

## Potential Gotchas / Risks

- Previous `apply_patch` attempt for handoff creation failed; prefer small patches or PowerShell here-strings for handoff files if patching remains brittle.
- The project may have uncommitted Chunk 17A/17B changes; avoid overwriting or broad refactors.
- The backend may open a real WIGE websocket when started; do not start/stop unrelated Node processes or interrupt possible WIGE recorder sessions.
- Visual browser QA is recommended after 17B.3/17C because hierarchy changes are spatial and first-viewport dependent.

## Pending Work / Backlog

- 17B.3: height/density tuning.
- 17B.4: responsive cleanup if needed.
- 17C: selected-car inspector/image treatment polish.
- 17D: final responsive/UX verification and chained handoff.
- Commit/push current Chunk 15-17 work when stable and intended.
